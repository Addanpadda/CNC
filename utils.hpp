#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>

namespace Utils {
    int findEndOfNumber(const std::string &str, const int &start);
    std::string clearSpaces(const std::string &str);
};

#endif